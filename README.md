# Playfair Cipher

Encrypts or decrypts using the Playfair cipher.

Example use:

    $ ./cli.py -k mykey -m message

For usage details:

    $ ./cli.py -h

## Features

- interactive mode
- verbose mode
- file encrypt or decrypt
